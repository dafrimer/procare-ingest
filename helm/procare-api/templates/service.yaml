apiVersion: v1
kind: Service
metadata:
  name: {{ include "procare-api.fullname" . }}
  labels:
    {{- include "procare-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "procare-api.selectorLabels" . | nindent 4 }}